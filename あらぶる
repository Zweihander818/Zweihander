float x, y;
void setup() {
  size(500,500 );
  x = 0;
  y = height/2; 
}

void draw() {
  background(255,204,0);
  x += 100;
  if (x > width) { 
    x = -100;
    y = random(width - 10); 
    
  }
  rect(x, y, 100, 100);
}
